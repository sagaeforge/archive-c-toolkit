
#include "ProgramManager.h"

struct ProgramManager Manager;

void ProgramManager_Init() {
  // FuncChain_Setting(&Manager.Init);
  // FuncChain_Setting(&Manager.Awake);
  // FuncChain_Setting(&Manager.Start);
  // FuncChain_Setting(&Manager.Quit);
}